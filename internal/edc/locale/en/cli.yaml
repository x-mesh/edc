# English messages for command execution and errors. This file is the base.
cli:
  usage: "usage: %s"
  confirm: "continue? [y/N] "
  config:
    invalid: "invalid config: %v"
  error:
    unknown_command: "unknown command: %s"
    help_hint: "run edc help to see the command list"
    host_port_required: "the target needs the host:port form: %s"
    one_target_required: "%s needs one target"
    no_positional: "%s takes no positional argument"
    profile_value: "--profile takes default or full"
    min_days_range: "--min-days must be 0 or more"
    expect_status_range: "--expect-status must be between 100 and 599"
  doctor:
    tls_skipped_for_http: "edc applies no TLS to an HTTP target"
  info:
    host_failed: "edc did not read the system information: %v"
    public_failed: "the public network lookup failed: %v"
  log:
    stream_value: "--stream must be stdout or stderr"
    output_required: "--output is required"
    output_file_required: "--output must name a file, not -"
    command_display_value: "--command-display must be full, name, or none"
    command_required: "a command is required after --"
    open_failed: "could not open log file %s: %v"
    lock_failed: "could not lock log file %s: %v"
    write_failed: "could not write log file %s: %v"
    sync_failed: "could not sync log file %s: %v"
    start_failed: "could not start command: %v"
    wait_failed: "could not wait for command: %v"
    signal_failed: "could not forward signal: %v"
  setup:
    title: "edc default setup"
    path: "config file: %s"
    language: "language (en, ko, ja)"
    preview: "The following config will be written to %s"
    save: "Save this config?"
    saved: "config saved: %s"
    cancelled: "edc cancelled setup"
    terminal_required: "edc setup requires an interactive terminal"
    path_unavailable: "the user config directory is unavailable"
    write_failed: "could not save config: %v"
    input_failed: "could not read setup input: %v"
    invalid_value: "invalid value: %v"
    clear_hint: "Enter keeps, !clear removes"
    recovering: "the current config is invalid; setup will replace it: %v"
    config_changed: "config changed in another process; run edc setup again"
    section:
      common: "Configure common defaults?"
      log: "Configure log defaults?"
      top: "Configure top defaults?"
      info: "Configure info defaults?"
      where: "Configure where defaults?"
      doctor: "Configure doctor defaults?"
      tls: "Configure TLS defaults?"
      http: "Configure HTTP defaults?"
      capture: "Configure capture defaults?"
      remote: "Configure remote defaults?"
      update: "Configure update defaults?"
    field:
      common_timeout: "common timeout"
      common_json: "common JSON path (empty disables)"
      common_verbose: "common verbose"
      common_redact: "common redact"
      log_stream: "log stream (stdout, stderr)"
      log_output: "log output file"
      log_command_display: "command display (full, name, none)"
      top_interval: "top interval"
      top_count: "top count (0 means unlimited)"
      top_no_header: "top no header"
      top_json: "top JSON path (empty disables)"
      info_public: "info public lookup"
      info_timeout: "info timeout"
      info_verbose: "info verbose"
      where_provider: "where provider (all, aws, gcp)"
      where_count: "where sample count"
      doctor_profile: "doctor profile (default, full)"
      tls_min_days: "TLS minimum remaining days"
      http_expect_status: "expected HTTP status"
      capture_interface: "capture interface (empty disables default)"
      capture_duration: "capture duration"
      capture_count: "capture packet count"
      capture_filter: "capture BPF filter"
      capture_output: "capture output path"
      remote_inventory: "remote inventory path"
      remote_recipe: "remote recipe path"
      remote_connect_timeout: "remote connection timeout"
      remote_output_limit: "remote output byte limit"
      remote_parallel: "remote parallel hosts (0 uses inventory)"
      update_timeout: "update timeout"
    validation:
      language: "use en, ko, or ja"
      yes_no: "answer y or n"
      positive: "must be greater than 0"
      non_negative: "must be 0 or more"
      stream: "use stdout or stderr"
      file_path: "enter a file path other than -"
      command_display: "use full, name, or none"
      top_interval: "must be at least 200ms"
      absolute_path: "use an absolute path"
      provider: "use all, aws, or gcp"
      profile: "use default or full"
      http_status: "must be 0 or between 100 and 599"
      capture_duration: "must be more than 0s and at most 60s"
      capture_count: "must be between 1 and 10000"
      remote_timeout: "must be more than 0s and at most 24h"
      remote_output_limit: "must be between 1 and 1048576"
      remote_parallel: "must be between 0 and 1000"
  capture:
    macos_only: "capture runs on macOS only"
    unexpected_positional: "edc found an unknown positional argument. Pass the BPF expression with --filter"
    interface_required: "--interface is required"
    duration_range: "--duration must be more than 0s and 60s at most"
    count_range: "--count must be between 1 and 10000"
    unknown_interface: "unknown interface: %s"
    output_exists: "edc keeps the existing file: %s"
    plan_title: "capture plan"
    payload_warning: "Warning: a packet payload can hold a credential or personal data."
    label:
      privilege: "privilege"
    privilege_root: "edc runs as root"
    privilege_sudo: "edc runs tcpdump with sudo"
    confirm: "Run the capture?"
    cancelled: "edc cancelled the capture"
    failed: "capture failed: %v"
    chmod_failed: "capture finished, but the permission change failed: %v"
    done: "capture finished: %s"
  report:
    unsupported_schema: "%s: unsupported schema version: %s"
    filter_label: "filter: "
    detail_open: "  ·  detail open"
    filter_empty: "no item matches this filter"
    viewer_help: "  ·  f filter, e detail, ↑/↓ move, q quit"
    filter:
      all: "all"
      fail: "fail only"
      problems: "fail and warn"
      changed: "changed"
      worse: "worse"
  update:
    no_tag: "the release carries no tag"
    already_latest: "edc %s is already the latest"
    versions: "current %s  ·  latest %s"
    asset_missing: "the release carries no asset for this platform: %s"
    checksums_missing: "the release carries no %s, so edc cannot check the checksum"
    not_writable: "edc cannot write to %s: %v"
    confirm: "Run the update?"
    cancelled: "edc cancelled the update"
    replaced: "edc updated to %s  ·  %s"
    label:
      current: "current"
      latest: "latest"
      asset: "asset"
      target: "target"
    release_read_failed: "edc did not read the release information"
    request_failed: "edc did not build the request"
    fetch_failed: "the request to %s failed"
    http_status: "%s returned HTTP %d"
    response_read_failed: "edc did not read the response"
    asset_too_large: "the asset passes the %d bytes limit"
    checksum_missing: "edc found no checksum of %s in %s"
    checksum_mismatch: "the checksum does not match: expected %s, got %s"
    asset_open_failed: "edc did not open the asset"
    asset_read_failed: "edc did not read the asset"
    binary_read_failed: "edc did not read the executable file"
    binary_empty: "the edc executable file in the asset is empty"
    binary_absent: "the asset carries no edc executable file"
    executable_path_failed: "edc did not find the path of the executable file"
    executable_resolve_failed: "edc did not resolve the path of the executable file"
    temp_create_failed: "edc did not create the temporary file"
    temp_write_failed: "edc did not write the temporary file"
    temp_close_failed: "edc did not close the temporary file"
    chmod_failed: "edc did not set the execute permission"
    rename_failed: "edc did not replace %s"
